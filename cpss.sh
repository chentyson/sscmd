ssh root@19j.boosoo.cn "echo =============19j;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19j2.boosoo.cn "echo =============19j2;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19j3.boosoo.cn "echo =============19j3;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19j4.boosoo.cn "echo =============19j4cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19u.boosoo.cn "echo =============19u;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19u2.boosoo.cn "echo =============19u2;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19u3.boosoo.cn "echo =============19u3;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@19u4.boosoo.cn "echo =============19u4;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@vip2.boosoo.cn "echo =============vip2;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@eam.vip.boosoo.cn "echo =============eam;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@yawoo.vip.boosoo.cn "echo =============yawoo;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@lktools.vip.boosoo.cn "echo =============lktools;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@yk.vip.boosoo.cn "echo =============yk;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
ssh root@v6.boosoo.cn "echo =============v6;cd ~/sscmd;git pull;systemctl start supervisord;supervisorctl stop ss;cp ./shadowsocks-server /usr/bin/shadowsocks-server;supervisorctl start ss"
